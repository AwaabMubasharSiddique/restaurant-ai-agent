# Dev/test dependencies (in addition to requirements.txt).
# The pure-logic tests are offline; agent/api tests run when the full stack is installed.
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
ruff>=0.6.0
pip-audit>=2.7.0
